@model  IEnumerable<MvcApplication2.Models.Account>
  


   @Html.ActionLink("Add", "Add");
   <br/>
   @foreach (var item in Model)
   {
       @Html.DisplayFor(asdf => item.Name)
       @Html.DisplayFor(asdf => item.Id)
       @Html.DisplayFor(asdf => item.Age)

       <br/>
}
